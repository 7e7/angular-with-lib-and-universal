import { JbFormFieldContainerComponent } from './jb-form-field-container.component';

describe('Component: JbFormFieldContainerComponent', () => {
  let component: JbFormFieldContainerComponent;

  beforeEach(() => {
    component = new JbFormFieldContainerComponent();
  });

  // This component has a lot going on and would be best tested with integration testing.
  it('should exist', () => {
    expect(component).toBeTruthy();
  });
});
